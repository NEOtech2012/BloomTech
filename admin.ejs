<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <link rel="icon" type="image/png" href="/images/bloom.png">
     <title>Admin Control | BloomTech</title>
    <link rel="stylesheet" href="/css/style.css">
    <script src="https://cdn.tailwindcss.com"></script>
</head>
<body class="bg-gray-100 font-sans">
    <div class="flex">
        <div class="w-64 h-screen bg-gray-900 text-white p-6 sticky top-0">
            <h2 class="text-2xl font-bold mb-8 text-blue-400">BloomTech Admin</h2>
            <nav class="space-y-4">
                <a href="/" class="block text-gray-400 hover:text-white">← View Site</a>
                <div class="border-t border-gray-800 my-4"></div>
                <p class="text-xs uppercase text-gray-500 font-bold">Management</p>
                <a href="#messages" class="block py-2 text-blue-400">Inquiries</a>
                <a href="#projects" class="block py-2 text-gray-400 hover:text-white">Projects</a>
            </nav>
        </div>

        <main class="flex-1 p-10">
            <h1 class="text-3xl font-bold mb-8 text-gray-800">Inbound Inquiries</h1>
            
            <div id="messages" class="bg-white rounded-xl shadow-sm border border-gray-200 overflow-hidden">
                <table class="w-full text-left">
                    <thead class="bg-gray-50 border-b border-gray-200">
                        <tr>
                            <th class="px-6 py-4 text-sm font-semibold text-gray-600">Sender</th>
                            <th class="px-6 py-4 text-sm font-semibold text-gray-600">Message</th>
                            <th class="px-6 py-4 text-sm font-semibold text-gray-600">Date</th>
                        </tr>
                    </thead>
                    <tbody class="divide-y divide-gray-100">
                        <% messages.forEach(msg => { %>
                            <tr class="hover:bg-gray-50 transition">
                                <td class="px-6 py-4">
                                    <p class="font-bold text-gray-800"><%= msg.sender_name %></p>
                                    <p class="text-xs text-gray-500"><%= msg.sender_email %></p>
                                </td>
                                <td class="px-6 py-4 text-sm text-gray-600"><%= msg.message_text %></td>
                                <td class="px-6 py-4 text-xs text-gray-400">
                                    <%= new Date(msg.created_at).toLocaleDateString() %>
                                </td><td class="px-6 py-4">
                                <td class="px-6 py-4">
    <p class="font-bold text-gray-800"><%= msg.sender_name %></p>
    <p class="text-xs text-blue-600 font-medium"><%= msg.sender_phone %></p> <p class="text-xs text-gray-500"><%= msg.sender_email %></p>
</td>
                            </tr>
                        <% }) %>
                    </tbody>

                    <thead class="bg-gray-50 border-b border-gray-200">
    <tr>
        <th class="px-6 py-4 text-sm font-semibold text-gray-600">Sender</th>
        <th class="px-6 py-4 text-sm font-semibold text-gray-600">Message</th>
        <th class="px-6 py-4 text-sm font-semibold text-gray-600">Action</th>
    </tr>
</thead>
<tbody class="divide-y divide-gray-100">
    <% messages.forEach(msg => { %>
        <tr class="hover:bg-gray-50 transition">
            <td class="px-6 py-4">
                <p class="font-bold text-gray-800"><%= msg.sender_name %></p>
                <p class="text-xs text-blue-600 font-medium"><%= msg.sender_phone %></p>
                <p class="text-xs text-gray-500"><%= msg.sender_email %></p>
            </td>
            <td class="px-6 py-4 text-sm text-gray-600"><%= msg.message_text %></td>
            <td class="px-6 py-4">
                <form action="/admin/delete-message/<%= msg.id %>" method="POST" onsubmit="return confirm('Archive this inquiry?');">
                    <button type="submit" class="text-red-500 hover:text-red-700 text-sm font-bold">
                        Delete
                    </button>
                </form>
            </td>
        </tr>
    <% }) %>
</tbody>
                </table>
            </div>
        </main>
    </div>
</body>
</html>